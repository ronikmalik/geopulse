name: Train shadow risk model

# Weekly, not daily — training is cheap at this data scale (see
# src/lib/riskModel.ts) but there's no reason to refit more often than
# new labeled examples can even appear (a snapshot's 14-day forward
# window doesn't resolve any faster than once/day).
# In-process on the runner since 2026-09-19 (see _run-job.yml) — training
# is the one place the old 55-120s Vercel maxDuration was actually tight;
# the runner has no such clock.
on:
  schedule:
    - cron: "0 12 * * 0"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  train:
    uses: ./.github/workflows/_run-job.yml
    with:
      job: train-risk-model
      timeout-minutes: 15
    secrets: inherit
